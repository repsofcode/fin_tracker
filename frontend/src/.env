# src/.env (place this in the ROOT of your frontend project, same level as package.json)

# Development — points to your local backend
VITE_API_URL=http://localhost:5000/api

# When you deploy, change this to your Render backend URL, e.g:
# VITE_API_URL=https://your-backend-name.onrender.com/api
